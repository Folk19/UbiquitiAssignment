version: '3'
services:
  postgres:
    container_name: postgres_db
    image: "postgres"
    env_file:
      - database.env
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/01-init.sql
    ports:
      - 5432